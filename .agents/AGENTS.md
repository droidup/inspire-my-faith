# Agent Rules

## Breadcrumbs
- When building or modifying breadcrumb navigation, always ensure that the "Home" link includes the `Home` icon (from lucide-react) to match the visual style of the application. (e.g. `<Home size={16}/> Home`)
